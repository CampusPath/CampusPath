//Interface to store user coordinates and allow them to be passed around between components
//  lat: The user's latitude
//  lng: The user's longitude

export interface Coords {
  lat: number;
  lng: number;
}
