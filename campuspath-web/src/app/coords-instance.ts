//instance of the coords interface

import { Coords } from './coords';
import { V1 } from './search';

export const COORDS: Coords = { lat: 0, lng: 0 }

export const ROUTE: V1.Route = {distance: 0, points: [[]]}